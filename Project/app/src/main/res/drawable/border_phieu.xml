<?xml version="1.0" encoding="utf-8"?>

<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <stroke
        android:width="3dp"
    android:color="@color/color_button" />

    <!-- Các thuộc tính sau đây định dạng hình dạng (hình tròn trong trường hợp này) -->
    <corners android:radius="5dp" />
    <solid android:color="@color/color_notice" />  <!-- Màu nền trong suốt (#00000000) -->
</shape>
